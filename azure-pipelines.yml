trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

- task: NodeTool@0
  inputs:
    versionSpec: '18.x'

- script: |
    cd app
    npm install
  displayName: Install Dependencies

- task: Docker@2
  inputs:
    command: build
    dockerfile: app/Dockerfile
